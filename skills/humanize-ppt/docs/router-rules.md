# Router Rules

- Chinese content + stability first → guizang path.
- Visual exploration + multiple directions → Zara path.
- Needs motion / inserted video → HyperFrames video slot.
- Needs live talk / speaker notes → Presenter Adapter after deck is finalized.
- Needs shareable URL / PDF → Deploy Adapter.
